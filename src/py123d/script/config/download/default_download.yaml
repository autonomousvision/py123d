hydra:
  output_subdir: null
  run:
    dir: .
  searchpath:
    - pkg://py123d.script.config
    - pkg://py123d.script.config.common
  job:
    chdir: False

defaults:
  - default_common
  - dataset: ???
  - _self_


logger_level: info  # "debug", "info", "warning", "error"
